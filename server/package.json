{
  "name": "server",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "build": "rm -rf build && cd ../client && yarn build && mv -f build ../server && rm -rf build",
    "start": "node index.js"
  },
  "dependencies": {
    "body-parser": "^1.20.3",
    "cors": "^2.8.5",
    "dotenv": "^6.1.0",
    "ejs": "^2.6.1",
    "express": "^4.21.0",
    "fastify-static": "^4.7.0",
    "mailgun-js": "^0.22.0"
  }
}
